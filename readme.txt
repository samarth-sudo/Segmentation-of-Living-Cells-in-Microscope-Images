ReadMe for main.py

This Python script performs image segmentation using adaptive 
thresholding with edge detection and evaluates the segmentation 
results against ground truth images using Intersection over Union 
(IoU) and Dice Coefficient metrics.



ReadMe for rename.py 

This Python script renames all files in a specified directory 
sequentially with an optional prefix.

You can run this code on any dataset folder but due to space complexity 
that the gradescope allows to upload I have just uploaded the dataset2.